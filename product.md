# BOOKMATE

## 1. 프로젝트 개요

**BOOKMATE**는 책을 읽은 후 자신의 생각을 정리하고, 같은 책을 읽은 다른 사람들과 질문과 생각을 나눌 수 있는 **AI 기반 공개 북클럽 웹 서비스**이다.

BOOKMATE의 핵심 구조는 다음과 같다.

> **책 1권(판본/ISBN) = 공개 북클럽 1개**

별도의 여러 그룹형 북클럽은 현재 MVP 범위에 포함하지 않으며, 하나의 책에는 하나의 공개 북클럽만 존재한다.

메인 화면은 **BOOKMATE WORLD** 인터랙티브 타운 구조로 구성되어 있으며, 열려 있는 공개 북클럽 목록(`01 열린 북클럽`), 책 검색 및 새 북클럽 개설(`02 북클럽 만들기`), 독후감 작성(`03 나의 생각 쓰기`), 개인 독서 기록(`04 나의 책장`)의 4가지 핵심 진입 동선을 제공한다. 누구나 자유롭게 북클럽에 입장하여 질문과 다른 독자의 생각을 읽을 수 있다.

---

# 2. 사용자 유형 및 권한 정책

BOOKMATE는 사용자를 두 유형으로 명확히 구분하여 권한을 제어한다.

### 2.1 비로그인 사용자

비로그인 사용자도 독서 커뮤니티의 지적 자산을 자유롭게 탐색할 수 있다.

**가능한 기능:**
- BOOKMATE 메인 페이지 접근 (BOOKMATE WORLD 비주얼 및 공간 마커 탐색)
- 열려 있는 북클럽 목록(`01 열린 북클럽`) 조회
- 기존 북클럽 입장
- 토론 질문 읽기
- 다른 독자가 작성한 공개 답변 읽기

**불가능한 기능 (로그인 필요):**
- 책 검색 (`02 북클럽 만들기`)
- 새로운 북클럽 OPEN (개설)
- 답변 작성 (`SUBMIT THOUGHT →`)
- 본인 답변 수정 (`EDIT`)
- 질문 좋아요(추천)
- 독후감 생성 (`03 나의 생각 쓰기` / `WRITE ESSAY`)
- 나의 책장 (`04 나의 책장` / 개인 독서 기록 조회 및 등록)
- AI 후속 토론 (⏸ Deferred)

---

### 2.2 로그인 사용자

로그인 사용자는 비로그인 사용자의 모든 읽기 권한에 더해, 능동적인 북클럽 활동과 AI 개인화 기능을 이용할 수 있다.

**추가 가능한 기능:**
- 책 검색 (`02 북클럽 만들기`)
- 새로운 북클럽 OPEN (동일 ISBN 북클럽이 없을 때 최초 개설)
- 공개 답변 작성 (항상 공개 저장)
- 본인이 작성한 답변 수정 (`EDIT`)
- 질문 좋아요(추천)
- 나만의 독후감 생성 (`03 나의 생각 쓰기`)
- 나의 책장 (`04 나의 책장` / 개인 독서 기록 조회, 등록, 수정, 삭제, 북클럽 바로가기)
- 마이페이지 조회 (내 정보, 내가 참여한 책 목록, 책별 내 생각 모아보기 및 북클럽/독후감/책장 연결)
- AI 후속 토론 (⏸ Deferred: 백엔드/서비스 코드는 보존, 현재 사용자 흐름에서는 비활성화)

---

# 3. 핵심 제품 원칙

## 3.1 책 1권 = 공개 북클럽 1개 (ISBN 기준)

동일한 책(동일 판본)에 대해 여러 개의 공개 북클럽을 만들지 않는다.

BOOKMATE의 책 식별 및 북클럽 매핑 우선순위는 다음과 같다.

```text
1. ISBN13 (최우선 식별자)
2. ISBN10 (ISBN13이 없을 경우)
3. title + author 정규화 키 (직접 입력 등 ISBN이 전혀 없는 경우의 fallback)
```

* **동일한 ISBN**: 이미 등록된 해당 책의 북클럽으로 바로 입장한다 (`ENTER BOOK CLUB →`).
* **서로 다른 ISBN**: 제목과 작가가 같더라도 다른 판본(개정판, 양장본 등)으로 보고 별도의 북클럽을 생성한다.
* **ISBN이 없는 직접 입력 도서**: 제목과 작가를 정규화한 키(`normalized_key`)를 기반으로 동일 북클럽 여부를 판단한다.

---

## 3.2 열린 북클럽의 정의

현재 MVP에서는 별도의 `book_clubs` 테이블을 만들지 않는다.
"책 1권 = 북클럽 1개" 원칙에 따라:

> **books 테이블에 등록되어 있고 questions 테이블에 질문이 1개 이상 존재하는 책**

을 '열린 북클럽(`OPEN BOOK CLUBS`)'으로 정의한다.

---

## 3.3 질문은 공유한다

북클럽의 질문은 같은 책을 읽는 모든 사용자가 볼 수 있다.

질문은 두 가지 출처가 있다.

```text
✨ AI가 생성한 질문 (신규 북클럽 OPEN 시 호스트 질문 5개)
👥 사용자가 직접 만든 질문
```

좋은 질문에는 로그인 사용자가 추천(좋아요)을 할 수 있다.

---

## 3.4 북클럽 첫 답변은 항상 공개로 저장한다

기존의 답변 비공개 선택 정책을 폐지한다. 북클럽 질문에 작성하는 첫 번째 답변은 **모든 독자가 함께 읽을 수 있도록 항상 공개로 저장**한다.

```text
질문
↓
내 첫 답변 작성 (로그인 계정의 display_name 사용)
↓
항상 공개 저장 (public_answers DB)
```

- 공개/비공개 선택 체크박스는 존재하지 않는다.
- 댓글마다 닉네임을 직접 입력받지 않고, 로그인 사용자의 `display_name`을 자동으로 사용한다.
- 다른 독자에게 공개된다는 안내문을 명확히 제공한다: `"이 답변은 다른 독자에게 공개됩니다."`

---

## 3.5 공개 토론과 개인 사유 영역의 분리 및 독후감 보호

북클럽의 첫 답변은 PUBLIC(공개)이지만, 내가 기록한 생각들을 엮어 완성하는 독후감은 PERSONAL(개인) 영역이다.

```text
[PUBLIC BOOK CLUB]
  공개 질문
    ↓
  내 첫 답변
    ↓
  public_answers에 공개 저장 (모든 독자가 읽고, 본인만 수정 가능)

[PERSONAL DIALOGUE & ESSAY]
  내 답변 기록 누적 (discussions 세션 상태)
    ↓
  나만의 개인 독후감 에세이 생성 (최초 질문 + 내 답변 기반)
    ↓
  (선택적 심화: AI 후속 토론은 ⏸ Deferred 상태로 보존)
```

- **독후감 생성**: 현재 사용자가 직접 작성한 생각(내 첫 답변, 질문 맥락)만을 엮어 맞춤형 독후감을 작성한다. 다른 독자의 공개 답변은 내 독후감에 일체 포함되지 않는다.
- **AI 후속 질문 (⏸ Deferred)**: 코드는 유지하되 현재 사용자 흐름에서는 비활성화되어 있다. 따라서 독후감 생성에 후속 질문/답변이 필수가 아니며, 최초 답변들만으로도 단단한 에세이가 완성된다.

---

# 4. 핵심 사용자 흐름

```text
[메인 접속 (누구나) — BOOKMATE WORLD]
  ├─ 01 열린 북클럽 ── 열려 있는 북클럽 목록 확인 (데미안, 이방인 등)
  │                    ├─ [비로그인] ── 기존 북클럽 입장 → 질문 및 다른 독자의 공개 댓글 읽기
  │                    └─ [로그인] ──── 기존 북클럽 입장 → 토론 참여
  │
  ├─ 02 북클럽 만들기 ─ [비로그인 시] 로그인 모달 유도
  │                    └─ [로그인 시] 책 검색 패널 노출 (SEARCH BOOKS)
                     ↓
                   책 제목 검색 (Kakao Book Search API / Gemini 호출 0회)
                     ↓
                      ├─ 이미 북클럽 존재: [ENTER BOOK CLUB →] (기존 데이터 재사용 / Gemini 0회)
                      └─ 북클럽 없음: [ENTER BOOK CLUB →] (책 등록 & Gemini 최초 1회 질문 생성)
  │
  ├─ 03 나의 생각 쓰기 ─ 기록된 생각이 있을 때 바로 독후감 스타일 선택으로 이동
  │
  └─ 04 나의 책장 ──── [비로그인 시] 로그인 모달 유도
                       └─ [로그인 시] 전용 책장 화면 (읽은 책 목록, 기록하기, 북클럽 바로가기)

[북클럽 토론 참여 흐름 (로그인 독자)]
                     ↓
                   [DISCUSS] 클릭 → 현재 질문 바로 아래 Accordion 열림 (Single-expand)
                     ↓
                   다른 독자의 공개 생각 확인
                     ↓
                    내 생각 작성 (`YOUR THOUGHT` / 로그인 display_name / 항상 공개 안내)
                      ↓
                    [SUBMIT THOUGHT →] 클릭
                      ↓
                    내 댓글 등록 완료 (토스트: "생각이 기록되었습니다.", 현재 Accordion 유지)
                      ↓
                    본인 댓글에는 [EDIT →] 제공 (인라인 수정 textarea → [SAVE →] / [CANCEL])
                      ↓
                    상단 독후감 버튼 또는 [03 나의 생각 쓰기] 클릭
                      ↓
                    내가 작성한 생각만을 엮어 완성도 높은 독후감 생성 (복사 / 스타일 변경)
                    *(선택적 심화: AI 후속 질문은 ⏸ Deferred 상태로 보존되어 현재 자동 진입하지 않음)*
```

---

# 5. Magical Moment

BOOKMATE의 가장 중요한 AI 경험은 다음 순간이다.

사용자가 자신의 생각을 작성하면 AI가 일반적인 질문을 무작위로 보여주는 것이 아니라,

> **방금 작성한 답변을 이해하고 그 생각을 더 깊게 만들 수 있는 후속 질문을 생성한다.**

예:

### 질문

> 주인공의 선택에 동의하나요?

### 사용자 답변

> 저는 동의하지 않아요. 주변 사람들에게 상처를 주면서까지 자신의 선택을 밀어붙였다고 생각해요.

### AI 후속 질문

> 그렇다면 주인공이 자신의 목표를 포기하지 않으면서도 주변 사람들을 배려할 수 있는 다른 선택은 무엇이었을까요?

AI가 답을 알려주는 것이 아니라 **사용자의 생각을 더 끌어내는 것**이 핵심이다.

*(※ 운영 참고: 이 AI 후속 질문 기능은 현재 기술 및 서비스 코드가 완비되어 있으나, 사용자가 질문에 첫 생각을 편안하게 남길 수 있도록 메인 사용자 흐름에서는 ⏸ Deferred(비활성화)되어 있으며, 향후 심화 사유를 원하는 사용자를 위한 선택적 확장 기능으로 재도입될 예정이다.)*

---

# 6. 주요 기능

## 6.1 책 검색 (로그인 사용자 전용)

책 검색은 **로그인 사용자만 가능**하다. (비로그인 사용자에게는 로그인 안내 제공)

### 검색 및 비용 방지 원칙
1. 로그인 사용자가 책 제목 입력 후 `[ SEARCH → ]` 클릭
2. 백엔드를 통해 카카오 도서 검색 REST API(`Kakao Book Search API`)를 호출하여 도서 목록 조회
3. **중요: 책 검색 자체는 절대 Gemini 질문 생성을 발생시키지 않으며, DB에도 아무것도 저장하지 않는다.**
4. 검색 결과 목록에 다음 정보를 카드 형태로 표시:
   * 책 표지 이미지 (`thumbnail`)
   * 책 제목 (`title`)
   * 작가명 (`authors`)
   * 출판사 (`publisher`)
   * ISBN (`isbn` - 13자리 우선 추출)

### 직접 입력 Fallback (검색 결과 없음 또는 수동 입력)
* 검색 결과가 없거나 사용자가 원하는 책을 찾지 못한 경우:
  * `"찾는 책이 없나요? 직접 입력하기"` 버튼 제공
  * 수동 입력 항목: `책 제목`(필수), `작가`(필수), `기억에 남는 장면/메모`(선택)
  * 직접 입력된 책은 ISBN이 없으므로 `normalized_key` 방식으로 북클럽을 식별한다.

---

## 6.2 기존 북클럽 입장 vs 신규 북클럽 OPEN 분기

검색 결과에서 사용자가 책을 선택했을 때, 해당 책의 식별자(ISBN 우선, fallback: normalized_key)를 기준으로 기존 북클럽 존재 여부를 확인한다.

### 1) 동일 ISBN의 기존 북클럽이 존재하는 경우
- 버튼: `[ ENTER BOOK CLUB → ]`
- 동작: 기존에 등록된 북클럽으로 바로 입장한다.
- 비용 방지: **Gemini API를 절대 호출하지 않으며(Gemini 호출 0회)**, 기존에 저장된 질문들을 공유한다.

### 2) 동일 ISBN의 기존 북클럽이 존재하지 않는 경우
- 버튼: `[ OPEN BOOK CLUB → ]`
- 안내: "아직 개설되지 않은 북클럽입니다. 첫 번째 호스트가 되어 북클럽을 열어보세요!"
- 동작: **사용자가 명시적으로 `OPEN BOOK CLUB →` 버튼을 클릭했을 때만** 신규 책을 DB에 등록하고 최초 1회 Gemini 질문을 생성한다.

---

## 6.3 Gemini 초기 질문 생성 정책

Gemini 초기 질문 생성은 다음 4가지 조건을 **동시에 모두 만족할 때에만** 실행한다.

1. **로그인 사용자**일 것
2. 사용자가 **`OPEN BOOK CLUB`을 명시적으로 클릭**했을 것
3. 동일 ISBN의 **기존 북클럽이 DB에 존재하지 않을 것**
4. 해당 책의 **`questions` 개수가 0개**일 것

생성된 약 5개의 토론 질문은 DB `questions` 테이블에 저장되며, 이후 입장하는 모든 사용자들은 이 질문들을 공유한다.

```text
검색만 수행      → Gemini 호출 0회 (카카오 검색 API만 호출)
기존 북클럽 입장 → Gemini 호출 0회 (기존 질문 목록 조회)
신규 북클럽 OPEN → 최초 1회만 Gemini 질문 생성 후 DB 저장
```

---

# 6.4 AI 질문 생성 및 관점

Gemini는 한 번의 요청으로 5가지 관점의 깊이 있는 토론 질문을 일괄 생성한다.

- **감상**: 책을 읽으며 가장 기억에 남은 장면이나 감정
- **인물**: 등장인물의 선택과 도덕적 딜레마
- **해석**: 작품이 우리 사회나 인간에게 던지는 핵심 질문
- **비판**: 등장인물이나 상황을 다른 관점에서 바라보기
- **나와 연결**: 책의 내용을 독자 자신의 삶, 가치관, 현실과 연결하기

---

# 6.5 북클럽 질문 목록 및 Accordion 토론 UI

책별 북클럽에서는 질문 목록을 보여주며, 질문 아래에서 바로 토론에 참여할 수 있다.

### Accordion 인터랙션 방식
- 각 질문 카드의 `[ DISCUSS ]` 버튼을 누르면 별도 페이지로 이동하지 않고, **현재 질문 바로 아래에 Accordion 패널이 부드럽게 펼쳐진다.**
- **Single-expand 원칙**: 한 번에 하나의 질문에 대한 Accordion만 열린다. 다른 질문의 `[ DISCUSS ]`를 누르면 기존 열린 패널은 자동으로 닫힌다.

---

# 6.6 질문 추천 (좋아요)

- **로그인 사용자만** 좋은 질문에 추천(👍)을 할 수 있다.
- 추천 수가 많은 질문은 우선 정렬되어 다른 독자들의 참여를 이끈다.
- *참고: 사용자 계정 기반 `question_likes` DB 전환은 후속 Phase에서 적용하며, 현재 전환 단계에서는 기존의 `localStorage` 기반 중복 방지를 유지한다.*

---

# 6.7 사용자 질문 추가

로그인 사용자는 자신이 생각한 새로운 토론 질문을 직접 제안하고 추가할 수 있다. (`+ 질문 제안`)
등록된 질문은 해당 책의 공개 북클럽에 `COMMUNITY` 태그로 추가된다.

---

# 6.8 다른 독자의 공개 답변 열람

Accordion 패널의 `OTHER READERS` 영역에서 해당 질문에 대해 다른 독자들이 남긴 공개 생각을 누구나(비로그인 포함) 자유롭게 읽을 수 있다.

---

# 6.9 내 답변 작성 (항상 공개 정책)

기존의 공개/비공개 선택 정책을 폐지한다. 북클럽 질문에 작성하는 첫 번째 답변은 **모든 독자가 함께 나누는 생각으로 항상 공개 저장**된다.

### UI 구성:
```text
YOUR THOUGHT

[ 답변 입력 텍스트에어리어 ]

이 답변은 다른 독자에게 공개됩니다.

[ POST → ]
```

- **제거된 요소**:
  - `☐ 다른 독자에게 내 생각 공개하기` 체크박스 제거
  - 댓글마다 직접 입력하던 `닉네임` 인풋 제거
- **작성자 표시**:
  - 로그인 계정의 `display_name`을 자동으로 작성자 이름으로 사용한다.
- **비로그인 사용자**:
  - 답변 입력창 대신 `"토론에 참여하고 내 생각을 남기려면 로그인하세요. [LOGIN →]"` 안내가 표시된다.

---

# 6.10 답변 수정 권한 (본인 답변 한정)

로그인 사용자는 **자신이 작성한 공개 답변만 수정(`EDIT`)**할 수 있다.

- **Frontend**: 내가 작성한 댓글 우측에만 `[ EDIT → ]` 버튼이 노출되며, 다른 사용자의 댓글에는 수정 버튼이 표시되지 않는다.
- **Backend 권한 강제**: 단순 UI 제어에 그치지 않고, 백엔드 수정 API(`PATCH /api/answers/{answer_id}`)에서 `현재 로그인 사용자 ID == 댓글 작성자 user_id` 여부를 엄격히 검증한다. (불일치 시 403 Forbidden)
- **기존 댓글 처리**: 로그인 도입 이전에 작성된 기존 댓글(`user_id IS NULL`)은 작성자를 확인할 수 없으므로, **모든 사용자가 읽을 수는 있지만 아무도 수정할 수 없는 불변의 기록**으로 안전하게 보호한다.
- **수정 이력**: MVP에서는 전체 수정 히스토리를 저장하지 않고 `updated_at` 타임스탬프만 갱신한다.

---

# 6.11 AI 후속 토론 (개인 영역) [⏸ Deferred / 현재 사용자 흐름에서 비활성화]

첫 답변 제출 후 진행되는 Gemini와의 대화는 다른 독자에게 공개되지 않는 **개인(PERSONAL) 영역**이다.

- **현재 적용 상태**:
  - 백엔드 엔드포인트 (`POST /api/ai/follow-up`), `ai_service` 로직, 프론트엔드 API 호출 함수(`BookMateAPI.getFollowUpQuestion`), `state.js`의 세션 저장 구조는 그대로 유지되어 있다.
  - 그러나 사용자가 질문에 첫 생각을 부담 없이 남길 수 있도록 **답변 등록(`SUBMIT THOUGHT →`) 후 자동으로 후속 질문으로 전환되는 사용자 흐름은 비활성화(Deferred)**되어 있다.
  - 답변 등록 후 화면은 현재 질문의 Accordion 상태를 유지한다.
- **향후 계획**: 1단계 생각 작성을 마친 사용자가 원할 때 1:1 심화 질문을 요청할 수 있는 선택적 버튼/옵션 형태로 재도입할 예정이다.

---

# 6.12 독후감 생성 (개인 생각 100% 기반)

사용자가 여러 질문에 답한 뒤 독후감을 생성할 수 있다.

- **원칙**: 오직 **현재 사용자가 직접 작성한 생각(최초 질문에 대한 내 답변, 질문 맥락)**만을 엮어 맞춤형 독후감을 완성한다. 다른 독자의 공개 답변은 내 독후감에 일체 포함되지 않는다.
- **Follow-up 독립성**: AI 후속 질문 답변 데이터가 필수가 아니며, **최초 질문과 내 답변만으로도 완성도 높은 독후감이 100% 정상 생성**된다. (과거 세션 등에 후속 토론 데이터가 존재하는 경우 프롬프트에 선택적으로 포함될 수 있음)
- **독후감 스타일**:
  * 자연스러운 개인 감상 (기본값)
  * 학교 과제 및 리포트
  * 블로그 리뷰
  * 비평 및 작품 분석
- **저장 방식**: 로그인 사용자 기반 영구 DB 저장은 후속 Phase에서 적용하며, 현재 전환 단계에서는 브라우저 `sessionStorage` 구조를 유지한다.

---

# 6.13 나의 책장 (개인 독서 기록 관리)

북클럽에 참여하지 않더라도 개인이 읽은 책과 짧은 감상을 나만의 아카이브로 남길 수 있는 전용 기능이다.

### 1) 북클럽과의 독립 및 도서 연결
- 기존 카카오 도서 검색을 재사용하여 책을 선택하거나 직접 입력한다.
- **Gemini 비용 방지**: 책장에 책을 등록할 때는 도서 메타데이터만 DB `books`에 저장하며, **Gemini 북클럽 질문 생성을 절대 호출하지 않는다 (Gemini 호출 0회).**
- 북클럽 질문이 생성되지 않은 책은 메인 `01 열린 북클럽` 목록에 노출되지 않아 북클럽 순수성이 유지된다.

### 2) 독서 기록 입력 항목
- **읽은 날짜 (`read_date`)**: 오늘 날짜를 기본값(`YYYY-MM-DD`)으로 제공하며 사용자가 수정 가능 (필수).
- **별점 (`rating`)**: 1~5점 별점 선택 (선택 입력).
- **한줄 감상 (`review`)**: 최대 200자 텍스트 입력 (선택 입력).

### 3) 중복 기록 방지 및 수정 모달
- 1인당 동일 도서는 1개의 독서 기록만 생성 가능하다 (`uq_reading_records_user_book` 유니크 제약).
- 이미 책장에 등록된 책을 다시 기록하려 할 경우, 기존 기록 확인 및 수정 모달로 안내하여 덮어쓰기 실수를 방지한다.
- 기록 카드의 `수정` / `삭제` 버튼을 통해 기존 기록을 언제든 인라인 모달로 갱신하거나 삭제할 수 있다.

### 4) 북클럽 바로가기 연동
- 책장의 각 카드에 `북클럽 가기 →` 버튼을 제공한다.
- 클릭 시 해당 책의 기존 북클럽으로 즉시 입장하거나, 아직 북클럽이 없는 경우 신규 북클럽 OPEN 단계로 자연스럽게 연결된다.

---

# 7. 데이터 공개 및 권한 정책 매트릭스

| 데이터 / 기능 | 공개 범위 | 저장 위치 | 비로그인 허용 | 로그인 필수 |
| :--- | :---: | :---: | :---: | :---: |
| **열린 북클럽 목록 (`OPEN BOOK CLUBS`)** | 공개 | `books`, `questions` DB | **O (조회)** | O |
| **기존 북클럽 입장 및 질문 열람** | 공개 | `books`, `questions` DB | **O (열람)** | O |
| **다른 독자의 공개 댓글 읽기** | 공개 | `public_answers` DB | **O (열람)** | O |
| **책 검색 (`SEARCH BOOKS`)** | - | 외부 Kakao API 프록시 | X | **O** |
| **새로운 북클럽 `OPEN`** | 공개 | `books`, `questions` DB | X | **O** |
| **질문 답변 작성** | **공개** | `public_answers` DB | X | **O** |
| **내 답변 수정 (`EDIT`)** | **공개** | `public_answers` DB | X | **O (본인만)** |
| **질문 추천 (좋아요)** | 공개 | `questions.likes` | X | **O** |
| **개인 독서 기록 (나의 책장)** | **개인 (비공개)** | `reading_records` DB | X | **O (본인만)** |
| **AI 후속 질문 및 후속 답변** | **개인 (비공개)** | 클라이언트 세션 (코드 보존) | X | **⏸ Deferred** (흐름 비활성화) |
| **개인 독후감 에세이** | **개인 (비공개)** | 클라이언트 세션 | X | **O** |

---

# 8. MVP 데이터 모델

BOOKMATE는 **Neon PostgreSQL**을 사용한다.

핵심 테이블은 다음과 같다.

```text
books
questions
public_answers
reading_records
```

---

# 8.1 books

```text
id
title
author
isbn
publisher
thumbnail_url
normalized_key
created_at
```

한 개의 `books` 데이터가 하나의 공개 북클럽을 의미한다.
별도의 `book_clubs` 테이블을 만들지 않는다.

```text
books.id = 공개 북클럽 식별자
```

---

# 8.2 questions

```text
id
book_id
content
source
likes
created_at
```

`source` 값:
- `AI` (신규 북클럽 OPEN 시 호스트 질문)
- `USER` (독자 제안 질문)

`book_id`를 이용하여 책과 질문을 연결한다.

---

# 8.3 public_answers

북클럽 질문에 독자가 작성한 첫 답변을 항상 공개로 저장하는 테이블이다.

```text
id
question_id
user_id        (작성자 ID, 기존 데이터 호환을 위해 NULL 허용)
nickname       (작성 시점 로그인 사용자의 display_name)
answer         (답변 본문)
created_at
updated_at     (수정 시점 기록, 미수정 시 NULL)
```

- **기존 데이터 보존**: 로그인 도입 이전 댓글(`user_id IS NULL`)은 삭제하지 않고 영구 보존하며, 읽기는 가능하지만 수정할 수는 없다.
- **수정 정책**: 로그인한 작성자 본인(`current_user.id == public_answers.user_id`)만 수정 가능하며, 수정 시 `updated_at`이 갱신된다.
- 개인 AI 후속 질문이나 독후감은 이 테이블에 저장하지 않는다.

---

# 8.4 reading_records

북클럽과 독립적으로 개인이 읽은 책과 감상을 기록하는 전용 테이블이다.

```text
id             UUID (PK)
user_id        VARCHAR (작성자 식별자, Neon Auth ID)
book_id        UUID (FK -> books.id)
read_date      DATE (읽은 날짜, 필수)
rating         SMALLINT (1~5점 별점, 선택)
review         VARCHAR(200) (한줄 감상 최대 200자, 선택)
created_at     TIMESTAMPTZ (기록 등록 시각)
updated_at     TIMESTAMPTZ (기록 수정 시각)
```

- **격리 및 소유권**: 오직 작성자 본인(`current_user.id == reading_records.user_id`)만 조회/수정/삭제할 수 있는 엄격한 개인 데이터이다.
- **중복 방지 제약**: `(user_id, book_id)` 조합에 유니크 제약조건을 두어 동일 사용자의 동일 도서 중복 기록을 방지한다.
- **북클럽 분리**: 책장 등록 시 도서(`books`) 정보만 연동되며, 북클럽 질문(`questions`) 생성이나 AI 호출은 발생하지 않는다.

---

# 9. 데이터 관계

```text
BOOK (책 1권 = 도서 메타데이터 / 북클럽)
 │
 ├── QUESTION (공유 토론 질문)
 │      │
 │      ├── PUBLIC ANSWER (공개 독자 답변, user_id 소유권)
 │      ├── PUBLIC ANSWER
 │      └── PUBLIC ANSWER
 │
 ├── QUESTION
 │      │
 │      └── PUBLIC ANSWER
 │
 └── READING RECORD (개인 독서 기록, user_id 개인 격리)
```

---

# 10. 동일 도서 판별 원칙

동일 도서 및 북클럽 매핑은 다음 우선순위로 판별한다.

```text
1. ISBN13 (최우선 식별자)
2. ISBN10 (ISBN13 부재 시)
3. title + author 정규화 키 (직접 입력 등 ISBN이 전혀 없는 경우의 fallback)
```

- **동일 ISBN**: 기존 북클럽으로 연결하며, Gemini 질문을 중복 생성하지 않는다.
- **다른 ISBN**: 같은 제목/작가라도 별도의 판본 북클럽으로 구분한다.

---

# 11. Gemini API 비용 방지 및 사용 원칙

불필요한 API 호출을 최소화하여 비용을 방지한다.

## 질문 생성
- **검색 시**: Gemini API 호출 **0회**
- **기존 북클럽 입장 시**: Gemini API 호출 **0회**
- **신규 북클럽 OPEN 시**: 로그인 사용자가 명시적으로 OPEN을 클릭했을 때만 **최초 1회** 약 5개의 질문을 일괄 생성하여 DB에 영구 저장 (이후 재사용).

## AI 후속 질문
- 사용자가 첫 답변을 등록했을 때만 호출한다.
- 개인 생각 확장을 위한 열린 질문 1개 생성 (DB 미저장).

## 독후감 생성
- 사용자가 독후감 만들기 버튼을 눌렀을 때 1회 호출한다.
- 현재 사용자의 개인 토론 내용만 전달하여 1편의 에세이 생성.

---

# 12. 기술 방향

MVP 기술 스택:

```text
Frontend
- HTML5
- CSS3 (Contemporary Editorial Design)
- Vanilla JavaScript (ES6+)

Backend
- Python 3.12
- FastAPI
- uv

Database & Auth
- Neon PostgreSQL
- Neon Auth (Managed Better Auth / HttpOnly Cookie 기반 세션)

AI
- Google Gen AI SDK (Gemini API)
```

---

# 13. 시스템 구조

```text
Browser (Vanilla JS / Session State)
        ↓  (HttpOnly Session Cookie)
FastAPI Backend (Dependencies 인증 검증)
       ↙        ↘
Neon PostgreSQL      Google Gemini API
(books/questions/
 public_answers)
```

---

# 14. 보안 원칙

1. **API Key 프론트엔드 노출 금지**: Gemini API Key, Neon DB 접속 정보 등은 백엔드 `.env`에서만 관리한다.
2. **서버 측 권한 강제**: 댓글 수정, 북클럽 OPEN, 댓글 작성 시 프론트엔드가 전달한 `user_id`를 신뢰하지 않고, 백엔드 인증 세션을 통해 확인된 사용자 ID만을 사용한다.
3. **XSS 방지**: 사용자 댓글 및 입력값은 `textContent` 등 안전한 DOM 조작 API로 렌더링한다.

---

# 15. 주요 화면

## 15.1 메인 화면 (BOOKMATE WORLD 에디토리얼 구조)

```text
┌─────────────────────────────────────────────────────────────────────────┐
│ BOOKMATE    READ · THINK · DISCUSS        [WRITE ESSAY]  [LOGIN → / LOGOUT]│
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ [TOWN HERO (좌측 약 34%)]      [BOOKMATE WORLD 일러스트 + 오버레이 (우측)]   │
│                                ┌───────────────────────────────────────┐│
│ CONTEMPORARY BOOK CLUB         │ [01 열린 북클럽]                        ││
│ 같은 책.                       │ 지금 열려 있는 대화에 참여하세요. ↓      ││
│ 다른 생각.                     │                                       ││
│                                │ [02 북클럽 만들기]                    ││
│ 책 속에서 발견한 질문에 답하고,    │ 찾고 있는 책을 검색해보세요. →          ││
│ 같은 책을 읽은 다른 독자의 시선을    │                                       ││
│ 마주하며 나만의 단단한 생각을     │ [03 나의 생각 쓰기]                     ││
│ 완성하세요.                    │ 나만의 글로 완성해보세요. →             ││
│                                │                                       ││
│                                │ [04 나의 책장]                        ││
│                                │ 읽은 책과 짧은 감상을 기록해보세요. →   ││
│                                └───────────────────────────────────────┘│
│                                                                         │
│ ── [01 클릭 시 펼쳐지는 수평 Strip: OPEN BOOK CLUBS] ─────────────────── │
│  01 데미안 (5Q·12T)   02 이방인 (5Q·8T)   03 노인과 바다 (5Q·14T)   [✕]   │
│                                                                         │
│ ── [02 클릭 시 펼쳐지는 SEARCH BOOKS 패널 (로그인 시)] ────────────────── │
│  BOOK TITLE [ 아몬드, 데미안... ] [ SEARCH → ]                          │
│                                                                         │
│ ── [04 클릭 시 전용 책장 화면으로 이동 (로그인 시)] ──────────────────── │
│  MY BOOKSHELF (내가 기록한 책 목록 및 + 책 기록하기)                    │
│                                                                         │
│ [비로그인 사용자 전용 하단 안내]                                         │
│ 새로운 북클럽을 열거나 토론에 참여하려면 로그인하세요. [ LOGIN → ]        │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 15.2 책별 북클럽 화면 및 질문 Accordion

```text
📖 데미안 / 헤르만 헤세 作
OPEN FORUM

Q 01. 『데미안』에서 가장 강렬하게 기억에 남은 장면은 무엇이었나요?
      COMMUNITY · 12 THOUGHTS      [👍 8]  [DISCUSS ↓]

┌─────────────────────────────────────────────────────────┐
│ (Accordion 펼쳐짐)                                       │
│                                                         │
│ OTHER READERS                                           │
│ ── EUNJI / 01                                [EDIT →]   │
│ 싱클레어가 어둠의 세계와 빛의 세계를 구분하던 유년 시절이...    │
│                                                         │
│ ── MINSU / 02                                           │
│ 크로머에게 휘둘리며 느끼던 불안감이 가장 인상 깊었습니다.       │
│                                                         │
│ ─────────────────────────────────────────────────────── │
│ YOUR THOUGHT                                            │
│ [ 나만의 생각을 적어보세요...                         ] │
│                                                         │
│ 이 답변은 다른 독자에게 공개됩니다.                        │
│ [ POST → ]                                              │
└─────────────────────────────────────────────────────────┘
```

---

## 15.3 독후감 생성 및 결과 화면

```text
나의 독후감
COMPOSED FROM YOUR PERSONAL DIALOGUE

손원평 작가의 『아몬드』를 읽고 난 후...
(현재 사용자가 직접 작성한 생각만을 엮은 단단한 에세이)

[ 독후감 본문 복사하기 ]
[ 스타일 변경 / 다시 생성 ]
[ 다른 질문 참여하기 ]
```

---

## 15.4 마이페이지 (MY READING NOTES) 화면

```text
┌─────────────────────────────────────────────────────────┐
│ ← 메인으로                                              │
│                                                         │
│ MY READING NOTES                                        │
│ 책마다 남겨둔, 나의 생각들.                              │
│ 함께 읽은 책과 그 안에 남긴 나의 목소리를 모았어요.      │
│                                                         │
│ [독] 은지 MEMBER                                        │
│      reader@bookmate.kr                                 │
│                                                         │
│ 01 내가 참여한 책                                       │
│ 생각을 남긴 책을 선택하면 내 답변을 다시 볼 수 있어요.   │
│ ┌──────────────┐ ┌──────────────┐                       │
│ │ [표지]       │ │ [표지] 선택됨│                       │
│ │ 소년이 온다  │ │ 데미안       │                       │
│ │ 한강 作      │ │ 헤르만 헤세  │                       │
│ │ 내 생각 2개  │ │ 내 생각 3개  │                       │
│ └──────────────┘ └──────────────┘                       │
│                                                         │
│ 02 내가 쓴 생각                                         │
│ 데미안 / 헤르만 헤세 作 · 총 3개의 생각                   │
│ [ 이 책으로 독후감 쓰기 ↗ ]                             │
│                                                         │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ Q1. 싱클레어가 알을 깨고 나오는 과정에서...          │ │
│ │ "자신의 세계를 파괴하지 않고는..."                   │ │
│ │ 2026.09.10  수정됨 (2026.09.11)  [북클럽에서 보기 ↗]│ │
│ └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## 15.5 나의 책장 (MY BOOKSHELF) 화면

```text
┌─────────────────────────────────────────────────────────┐
│ ← 메인으로                                              │
│                                                         │
│ MY BOOKSHELF                                            │
│ 기록한 책, 기억할 문장.                                  │
│ 북클럽에 참여하지 않아도 나만의 독서 기록을 남겨보세요.   │
│                                                         │
│                                        [ + 책 기록하기 ] │
│ ─────────────────────────────────────────────────────── │
│                                                         │
│ ┌──────────────────────┐ ┌──────────────────────┐       │
│ │ [표지] 베로니카, ... │ │ [표지] 데미안        │       │
│ │ 파울로 코엘료 作     │ │ 헤르만 헤세 作       │       │
│ │ 📅 2026-09-15        │ │ 📅 2026-09-12        │       │
│ │ ★★★★★ (5/5)         │ │ ★★★★☆ (4/5)         │       │
│ │ "죽음 앞에서 비로소..│ │ "알을 깨고 나오는..."│       │
│ │                      │ │                      │       │
│ │ [수정] [삭제]        │ │ [수정] [삭제]        │       │
│ │ [ 북클럽 가기 → ]    │ │ [ 북클럽 가기 → ]    │       │
│ └──────────────────────┘ └──────────────────────┘       │
│                                                         │
│ [빈 상태 (기록이 없을 때)]                              │
│ 아직 책장에 담긴 책이 없어요.                            │
│ 읽은 책과 짧은 감상을 남겨보세요.                        │
│ [ + 책 기록하기 ]                                       │
└─────────────────────────────────────────────────────────┘
```

---

# 16. MVP에서 구현하지 않는 기능

MVP 범위를 작고 단단하게 유지하기 위해 다음 기능은 구현하지 않는다.

```text
비공개 북클럽 (초대 코드 기반 그룹)
그룹 관리자 페이지
실시간 채팅 및 DM
댓글의 대댓글 (1단계 토론 유지)
댓글 수정 이력 전체 버전 관리 (updated_at만 기록)
결제 및 유료 기능
팔로우 및 소셜 피드
```

---

# 17. MVP 완료 기준

다음 흐름이 처음부터 끝까지 정상적으로 동작하면 MVP가 완료된 것으로 본다.

```text
1. 비로그인 사용자가 메인 페이지에서 열려 있는 북클럽 목록을 확인한다.
2. 기존 북클럽에 입장하여 질문과 다른 독자의 공개 댓글을 자유롭게 읽는다.
3. 로그인 후 책 제목을 검색한다. (검색 시 Gemini 호출 0회)
4. 이미 존재하는 북클럽은 [ENTER BOOK CLUB]으로 입장하여 기존 데이터를 재사용한다. (Gemini 호출 0회)
5. 존재하지 않는 책은 [OPEN BOOK CLUB]을 명시적으로 클릭하여 신규 북클럽을 개설한다. (Gemini 최초 1회 호출)
6. 북클럽에서 [DISCUSS]를 클릭하여 현재 질문 바로 아래 Accordion을 연다.
7. 내 답변을 작성하면 항상 공개로 DB에 저장되며, 로그인 사용자의 display_name이 표시된다.
8. 내가 작성한 댓글에는 [EDIT] 버튼이 노출되며, 본인 댓글만 인라인으로 수정할 수 있다.
9. 첫 답변 등록 후 생각이 안전하게 기록되며 현재 질문의 Accordion 상태가 유지된다. (AI 후속 질문은 Deferred 처리)
10. 내가 작성한 생각 데이터(최초 질문 및 내 답변)를 기반으로 맞춤형 독후감을 100% 정상 생성한다.
11. 로그인 사용자가 Header의 사용자명을 눌러 마이페이지로 이동하고, 내가 참여한 책과 작성한 생각들을 모아보며 북클럽 및 독후감 작성으로 연결된다.
12. 메인 '04 나의 책장' 또는 마이페이지 버튼을 통해 전용 책장으로 이동하고, 읽은 책 검색 선택 → 읽은 날짜, 별점, 한줄 감상을 기록·수정·삭제하며, '북클럽 가기'로 자연스럽게 연결된다.
```

---

# 18. 향후 확장 방향

MVP 완료 후 다음 기능을 검토할 수 있다.

## 독서 통계 및 장르별 태그 분석

개인 책장의 연간 독서량, 월별 기록 추이 및 선호 장르 태그 통계를 제공한다.

## 비공개 북클럽

특정 독서모임만 참여하는 초대 코드 기반 북클럽을 추가한다.

## AI 의견 요약

여러 사용자의 공개 답변을 분석하여 주요 의견 차이를 요약한다.

## 다른 관점 추천

현재 사용자의 의견과 다른 공개 답변을 보여주어 새로운 관점에서 생각하도록 돕는다.

## AI 심화 후속 질문 재도입 (선택적 심화 대화)

현재 비활성화(Deferred)된 AI 후속 질문을 사용자가 원할 때 명시적으로 호출할 수 있는 1:1 심화 사유 옵션으로 재도입한다.

위 기능은 MVP 구현 범위에 포함하지 않는다.

---

# 19. BOOKMATE의 핵심 가치

BOOKMATE는 AI가 독후감을 대신 만들어주는 서비스가 아니다.

핵심 흐름은 다음과 같다.

> **책 → 같은 책의 북클럽 → 좋은 질문 → 다른 사람의 생각 → 내 생각 → AI 후속 질문 → 더 깊어진 생각 → 나만의 독후감**

궁극적으로 사용자가 책을 읽고 난 뒤,

> **“이 책의 내용이 무엇이었지?”**

에서 끝나는 것이 아니라,

> **“나는 이 책을 어떻게 생각했지?”**

를 발견할 수 있도록 돕는 것을 목표로 한다.
